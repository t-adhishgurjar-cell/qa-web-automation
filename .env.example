# Copy this file to .env.dev / .env.staging / .env.prod and fill in values
# Never commit real credentials — use CI secrets in pipelines

ENV=dev
BASE_URL=https://webqa.fleetforc.com
API_URL=https://webqa.fleetforc.com

# Test credentials (overridden by Excel sheet — FleetPlus_TestCredentials (1).xlsx)
TEST_USERNAME=9100000001
TEST_PASSWORD=Nayara@124

# 2FA / Captcha (QA environment)
TEST_OTP=123456
TEST_CAPTCHA=

# Logging
LOG_LEVEL=debug

# Timeouts (ms)
DEFAULT_TIMEOUT=60000
ACTION_TIMEOUT=15000
